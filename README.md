# XPC Hackathon - Personal Health Records Extension Demo

It is recommended to use a python venv:

```bash
python3 -m venv .venv
source .venv/bin/activate
```

Install dependencies:

```bash
pip install -r requirements.txt
```

To run:

```bash
uvicorn main:app --reload
```
